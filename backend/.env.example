# External AI providers (never exposed to the frontend)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

DEFAULT_PROVIDER=openai
DEFAULT_OPENAI_MODEL=gpt-4o-mini
DEFAULT_ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Session store (in-memory for MVP; back with Redis in production using the
# same "session:{id}:mapping" key schema described in the architecture docs)
SESSION_TTL_SECONDS=3600

# Limits
MAX_FILE_SIZE_MB=25
MAX_CHAT_LENGTH=8000
MAX_DOCUMENT_LENGTH=200000
RATE_LIMIT_PER_MINUTE=60

# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
